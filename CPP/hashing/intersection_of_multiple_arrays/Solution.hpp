#pragma once

#include <iostream>
#include <vector>
#include <map>
#include <unordered_map>
#include <algorithm>

class Solution {
    public:
        std::vector<int> intersection(const std::vector<std::vector<int>>& nums);
};