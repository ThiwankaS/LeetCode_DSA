#pragma once

#include <iostream>
#include <string>
#include <vector>
#include <unordered_map>

class Solution {
    public:
        std::string destCity(const std::vector<std::vector <std::string>>& paths);
};
