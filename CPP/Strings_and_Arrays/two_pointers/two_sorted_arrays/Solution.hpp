#include <iostream>
#include <vector>

class Solution {
    public:
        std::vector<int> concatinateSortedArrays(const std::vector <int>& arr1, const std::vector <int>& arr2);
        void printArray(const std::vector <int>& arr);
};